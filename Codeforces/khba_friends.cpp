//https://codeforces.com/contest/2167/problem/E
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t; cin >> t;
    while(t--){
        int n, k , x;
        cin >> n >> k >> x;
        vector<int> post(n);
        for(auto &it : post){
            cin >> it;
        }
    }
    return 0;
}